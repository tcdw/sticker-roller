---
id: e2e-001
scope: Web UI end-to-end integration and delivery verification
status: done
depends-on: [api-001, ui-001]
---

# Objective

Connect the production Bun server to the built Rsbuild frontend and verify the full durable workflow with real module boundaries.

## Context

- `docs/INDEX.md`
- `docs/web/README.md`
- `docs/web/workspace-layout.md`
- `docs/plan/analysis/web-ui.md`
- All predecessor task files and implementation docs.

## Path

- `web-server.ts`/`server/**`
- `web/**`
- `README.md`
- `package.json`
- `docs/**` only for consistency corrections
- `docs/plan/tasks/e2e-001.md`

Do not modify `stickers/` or `stickers_archived/`.

## Requirements

- Ensure `bun run build` creates a production frontend that the Bun server serves with API fallback and SPA routing.
- Verify a job remains active after the browser client is gone and is visible after a new client loads.
- Verify SQLite history, per-item progress, output delivery, retry/cancel, and restart recovery through real implementations.
- Preserve CLI commands and ensure no filesystem migration was introduced.

## Verification

Run `bun test`, typecheck, production build, and a local smoke test. Record any non-blocking findings in `docs/plan/backlog.md`; blocking findings must be fixed before pass.
