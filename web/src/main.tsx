import React, { useState } from "react";
import { createRoot } from "react-dom/client";
import { QueryClient, QueryClientProvider, useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { Outlet, RouterProvider, createRootRoute, createRoute, createRouter } from "@tanstack/react-router";
import { api, isActive, referencedIdsFromPrompt, useDraft, type Job } from "./api";
import { Button } from "./components/ui/Button"; import { Card } from "./components/ui/Card"; import { Badge } from "./components/ui/Badge"; import { Select } from "./components/ui/Select"; import { Dialog } from "./components/ui/Dialog"; import { Composer } from "./components/Composer"; import type { AssetRow, JobRow } from "../../src/web-types"; import "./styles.css";
const client=new QueryClient(); const rootRoute=createRootRoute({component:()=> <Outlet/>}); const workspaceRoute=createRoute({getParentRoute:()=>rootRoute,path:"/",component:Workspace}); const router=createRouter({routeTree:rootRoute.addChildren([workspaceRoute])}); declare module "@tanstack/react-router" { interface Register { router:typeof router } }
function Workspace(){ const qc=useQueryClient(); const draft=useDraft(); const assets=useQuery({queryKey:["assets"],queryFn:api.assets}); const jobs=useQuery({queryKey:["jobs"],queryFn:api.jobs,refetchInterval:q=>q.state.data?.some(j=>isActive(j.status))?1500:false}); const [dialog,setDialog]=useState<"create"|AssetRow|null>(null); const [params,setParams]=useState(false); const [notice,setNotice]=useState(""); const save=useMutation({mutationFn:({id,body}:{id?:string;body:{name:string;prompt:string;category:string}})=>id?api.updateAsset(id,body):api.createAsset(body),onSuccess:()=>{setDialog(null);qc.invalidateQueries({queryKey:["assets"]});setNotice("素材已保存")},onError:e=>setNotice((e as Error).message)}); const archive=useMutation({mutationFn:api.archiveAsset,onSuccess:()=>{setDialog(null);qc.invalidateQueries({queryKey:["assets"]});setNotice("素材已归档")},onError:e=>setNotice((e as Error).message)}); const submit=useMutation({mutationFn:()=>{const currentAssets=assets.data??[]; return api.createJob({authoredPrompt:draft.prompt,referencedAssetIds:referencedIdsFromPrompt(draft.prompt,currentAssets),...draft.options})},onSuccess:()=>{qc.invalidateQueries({queryKey:["jobs"]});draft.reset();setNotice("任务已提交")},onError:e=>setNotice((e as Error).message)}); const loading=assets.isLoading||jobs.isLoading; return <div className="app"><header><div className="brand">生图<br/><span>工作室</span></div><div><p className="eyebrow">LOCAL CREATIVE WORKBENCH</p><h1>把素材组合成任务</h1></div><Button variant="ghost" onClick={()=>{assets.refetch();jobs.refetch()}}>↻ 刷新</Button></header><main>{(assets.isError||jobs.isError)&&<p className="error">加载失败：{(assets.error||jobs.error as Error).message} <Button variant="secondary" onClick={()=>{assets.refetch();jobs.refetch()}}>重试</Button></p>}<Composer assets={assets.data??[]} onCreate={()=>setDialog("create")} onEdit={asset=>setDialog(asset)}/><Card className="toolbar"><Select disabled={loading} value={draft.options.model} onChange={e=>draft.set({options:{...draft.options,model:e.target.value}})}><option>gemini-3-pro-image</option><option>gemini-3.1-flash-image-preview</option></Select><Select disabled={loading} value={`${draft.options.aspectRatio} / ${draft.options.imageSize}`} onChange={e=>{const [aspectRatio,imageSize]=e.target.value.split("/");draft.set({options:{...draft.options,aspectRatio:aspectRatio!.trim(),imageSize:imageSize!.trim()}})}}><option>1:1 / 1K</option><option>1:1 / 2K</option><option>16:9 / 2K</option><option>9:16 / 2K</option></Select><Button variant="secondary" disabled title="Phase 1 仅支持文本素材">添加图片（暂不可用）</Button><Button variant="secondary" onClick={()=>setParams(true)}>更多参数</Button><Button disabled={submit.isPending||!draft.prompt.trim()} onClick={()=>submit.mutate()}>{submit.isPending?"提交中…":"提交任务"}</Button></Card>{notice&&<p className="notice">{notice}</p>}<History jobs={jobs.data??[]} loading={jobs.isLoading}/></main><MaterialDialog value={dialog} onClose={()=>setDialog(null)} onSave={(id,body)=>save.mutate({id,body})} onArchive={id=>archive.mutate(id)}/><ParametersDialog open={params} options={draft.options} onClose={()=>setParams(false)} onSave={options=>{draft.set({options});setParams(false)}}/></div> }
function MaterialDialog({value,onClose,onSave,onArchive}:{value:"create"|AssetRow|null;onClose:()=>void;onSave:(id:string|undefined,b:{name:string;prompt:string;category:string})=>void;onArchive:(id:string)=>void}){const asset=value!=="create"?value:null; const [name,setName]=useState("");const [prompt,setPrompt]=useState("");const [category,setCategory]=useState("提示词"); React.useEffect(()=>{setName(asset?.name??"");setPrompt(asset?.prompt??"");setCategory(asset?.category??"提示词")},[asset?.id,value]); return <Dialog open={value!==null} title={asset?"编辑素材":"创建可复用素材"} onClose={onClose}><label>名称<input value={name} onChange={e=>setName(e.target.value)}/></label><label>内容<textarea value={prompt} onChange={e=>setPrompt(e.target.value)}/></label><label>分组<input value={category} onChange={e=>setCategory(e.target.value)}/></label><Button disabled={!name.trim()||!prompt.trim()} onClick={()=>onSave(asset?.id,{name,prompt,category})}>保存素材</Button>{asset&&<Button variant="danger" onClick={()=>onArchive(asset.id)}>归档素材</Button>}</Dialog>}
function ParametersDialog({open,options,onClose,onSave}:{open:boolean;options:Job["options"] & {count:number;removeBackground:boolean};onClose:()=>void;onSave:(o:any)=>void}){const [count,setCount]=useState(options.count);const [removeBackground,setRemoveBackground]=useState(options.removeBackground);React.useEffect(()=>{setCount(options.count);setRemoveBackground(options.removeBackground)},[open,options]);return <Dialog open={open} title="更多参数" onClose={onClose}><label>生成张数<input type="number" min="1" max="20" value={count} onChange={e=>setCount(Math.max(1,Math.min(20,Number(e.target.value))))}/></label><label><input type="checkbox" checked={removeBackground} onChange={e=>setRemoveBackground(e.target.checked)}/> 移除背景</label><Button onClick={()=>onSave({...options,count,removeBackground})}>应用参数</Button></Dialog>}
function History({jobs,loading}:{jobs:JobRow[];loading:boolean}){if(loading)return <section className="history"><div className="empty">正在加载任务…</div></section>;if(!jobs.length)return <section className="history"><h2>任务历史</h2><div className="empty">提交任务后，图片结果会显示在这里</div></section>;return <section className="history"><div className="history-title"><h2>任务历史</h2><span>{jobs.length} 个任务</span></div>{jobs.map(j=><JobCard key={j.id} job={j}/>)}</section>}
function JobCard({job}:{job:JobRow}){const qc=useQueryClient();const detail=useQuery({queryKey:["job",job.id],queryFn:()=>api.job(job.id),refetchInterval:isActive(job.status)?1500:false});const cancel=useMutation({mutationFn:api.cancel,onSuccess:()=>qc.invalidateQueries({queryKey:["jobs"]})});const retry=useMutation({mutationFn:api.retry,onSuccess:()=>qc.invalidateQueries({queryKey:["jobs"]})});const full=detail.data;return <article className="job"><div className="history-title"><div><strong>{new Date(job.createdAt).toLocaleString()}</strong><small>（{job.requestedCount} 张）</small></div><Badge className={`status-${job.status}`}>{job.status}</Badge></div><p>{job.promptSnapshot}</p><div className="results">{(full?.items??[]).map(item=><div className="result" key={item.id}>{item.status==="succeeded"&&item.files?.[0]?<img src={`/api/output/${encodeURIComponent(item.files[0].fileName)}`} alt="生成结果"/>:<span>{item.status==="failed"?`失败：${item.error??"未知错误"}`:item.status}</span>}</div>)}</div><div className="job-actions">{isActive(job.status)&&<Button variant="secondary" onClick={()=>cancel.mutate(job.id)}>取消</Button>}{job.status==="failed"&&<Button variant="secondary" onClick={()=>retry.mutate(job.id)}>重试失败项</Button>}</div></article>}
function App(){return <QueryClientProvider client={client}><RouterProvider router={router}/></QueryClientProvider>};createRoot(document.getElementById("root")!).render(<App/>);
