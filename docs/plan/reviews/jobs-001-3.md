# jobs-001 Review 3

## Judgment

**pass**。复审 2 中的两个 P1 已修复，且本轮定向测试通过：

- `safeError()` 现在会替换完整的 `Authorization: Bearer <secret>` / `Basic <secret>` credential，而不是只替换 `Bearer` 词；错误持久化路径中的 bearer、basic、x-api-key secret 均有测试证明不会写入。
- claim 后、创建 request 前的取消窗口已由数据库事务边界关闭：`cancelJob()` 会把没有 running request 的 `running` item 置为 `cancelled`，随后 `startRequest()` 只允许仍为 `running` 且 job 未取消的 item，因此 provider `generate()` 不会被调用。

未发现新的 blocking finding。

## Findings

### Blocking

无。

### 已修复项确认

#### P1 — `Authorization: Bearer` 脱敏泄漏：通过

- [src/jobs/worker.ts:10-18](../../../src/jobs/worker.ts#L10) 的脱敏逻辑先移除 provider URL，再以完整 credential 值匹配敏感 header/key；其中 [worker.ts:16](../../../src/jobs/worker.ts#L16) 覆盖 `authorization`、`x-api-key` 等 `key=value` / `key: value` 形式，[worker.ts:17](../../../src/jobs/worker.ts#L17) 覆盖裸的 `Bearer` / `Basic` credential。
- [src/jobs/worker.test.ts:26-34](../../../src/jobs/worker.test.ts#L26) 使用 `Authorization: Bearer TOPSECRET, Basic BASICSECRET x-api-key=KEYSECRET` 触发 generator 错误，并断言 request.error 不包含三个 secret；同时确认 item 进入 `failed`。
- worker 的失败路径在 [worker.ts:72-75](../../../src/jobs/worker.ts#L72) 将脱敏后的错误交给 `finalizeRequest()`，因此 request/item 的持久化错误不会包含该测试中的 raw credentials。

#### P1 — claim 后、provider 调用前取消：通过

- [src/db/repositories.ts:77](../../../src/db/repositories.ts#L77) 的 `cancelJob()` 对 `running` item 仅在不存在 running `llm_requests` 时执行取消；queued item 仍可直接取消。
- [src/db/repositories.ts:42](../../../src/db/repositories.ts#L42) 的 `startRequest()` 在同一数据库事务中重新读取 item/job，要求 item 仍是 `running`，并拒绝已取消 job。取消先提交后，item 已是 `cancelled`，故不会创建 request。
- [src/jobs/worker.ts:56-62](../../../src/jobs/worker.ts#L56) 将取消导致的 `startRequest()` 错误视为终态返回，provider 调用发生在其后的 [worker.ts:67](../../../src/jobs/worker.ts#L67)。
- [src/jobs/worker.test.ts:36-43](../../../src/jobs/worker.test.ts#L36) 用包装后的 `claimNextItem()` 在 claim 返回后立即调用 `cancelJob()`，断言 provider calls 为 0、item 为 `cancelled`、request 数量为 0。
- 数据库层也有对应断言：[src/db/repositories.test.ts:48-55](../../../src/db/repositories.test.ts#L48) 验证 claimed item 被取消后 `startRequest()` 被拒绝且不会产生 request。

#### 复审 2 已通过项：保持通过

- `finalizeRequest()` 仍在 [src/db/repositories.ts:45-60](../../../src/db/repositories.ts#L45) 使用单一 SQLite transaction 终结 request、注册文件、终结 item、刷新 job 状态并记录事件。
- [src/jobs/worker.ts:35-43](../../../src/jobs/worker.ts#L35) 的 `start()` 先做 stale recovery，再启动后台 drain；stop 会等待 active provider call 和 drain loop。
- 已注册文件复用路径仍在 [src/jobs/worker.ts:53-55](../../../src/jobs/worker.ts#L53) 先检查 registry 与文件存在，再调用 `completeRegisteredItem()`，不会重新调用 provider。

## Verification evidence

- 执行：`bun test src/jobs/worker.test.ts src/db/repositories.test.ts server/api.test.ts`
- 结果：**15 pass，0 fail，59 expect() calls**。
- 覆盖内容包括：成功/失败/重试、queued cancellation、完整敏感 credential 脱敏、claim 后取消窗口、stale recovery，以及 DB transition/cancellation 保护。
- 执行：`bun run typecheck`
- 结果：仍失败于 `src/config.ts:119`、`src/config.ts:125`、`src/config.ts:265-268` 的既有 TypeScript 错误；本轮未发现 jobs worker/repository 新增 typecheck 错误，且该失败不构成本修复的新增 blocking finding。
- 本轮未修改 `stickers/` 或 `stickers_archived/`。

## Remaining uncertainty

- 文件 rename 与 SQLite finalize 之间的 crash orphan 文件清理策略仍未定义；这不影响本轮已验证的 DB 状态原子性。
- 已注册输出目前验证文件存在，但未额外校验 regular file、内容完整性或 registry metadata 与磁盘文件一致性；现有合同未明确这些要求。
- 取消测试通过受控顺序模拟 claim 后取消，而非多线程强制抢占；不过取消与 `startRequest()` 各自使用 SQLite transaction，且 startRequest 在事务内重新读取状态，关键安全边界已有数据库层证据。

## Priority summary

| Priority | Blocking | Non-blocking / uncertainty |
|---|---|---|
| P1 | — | — |
| P2 | — | orphan 文件清理；已注册文件完整性验证；全量 typecheck |

**Final: pass**
