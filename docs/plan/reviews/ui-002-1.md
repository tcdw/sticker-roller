# Web UI review follow-up

- The initial UI implementation incorrectly treated an asset as the entire task prompt. This was corrected in ui-002: assets are reusable material references and the prompt composer is independent.
- The initial UI did not use shadcn/ui. ui-002 added local shadcn/ui-compatible primitives under `web/src/components/ui/` and imports them in the workspace.
- The supplied prototype is now the source of truth for the two-column materials/task canvas hierarchy.
